import { gettext } from 'i18n'

AppSettingsPage({
  build(props) {
    return Section({}, [
      Section(
        {},
        Text({
        })
        
      ),
      Section(
        {},
        Text({
          paragraph: 'Server: ',
        })
      )
    ])
  }
})